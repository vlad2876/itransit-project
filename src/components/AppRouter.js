import React from 'react';
import {Route, Switch, Redirect} from "react-router-dom";

const AppRouter = () => {
    const user = false
    return user ?
        (
            <Switch>

            </Switch>
        )
        :
        (
            <Switch>

            </Switch>
        )
};

export default AppRouter;